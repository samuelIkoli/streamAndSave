{
  "name": "streamandsave",
  "version": "1.0.0",
  "description": "Endpoint to upload a video to using Rabbit MQ to send it in streams, upload it using the file system module, and transcribe the audio.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js"
  },
  "author": "Samuel Ikoli",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.10.3",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "uuid": "^9.0.1"
  }
}
